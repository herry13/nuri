schema Package {
	final package_name = ""
	installed : Bool
	version : String

	synchronized sub install {
		effect {
			this.installed = true
		}
	}

	synchronized sub uninstall {
		condition {
			this.installed = true
		}
		effect {
			this.installed = false
		}
	}
}
