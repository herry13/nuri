include "svcref-type.sfp"

initial state {
	servers isa Server[2] {
		running is true
	}
	a isa Server
	clients isa Client[2] {
		refer = servers[0]
	}
}
goal constraint {
	foreach (servers as x) {
		x.running = false
	}
}
global constraint {
	foreach (clients as c) {
		c.refer.running = true
	}
}
